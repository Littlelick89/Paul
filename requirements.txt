# ── Claude API mode ──────────────────────────────────────────────────────────
anthropic>=0.40.0

# ── Core ─────────────────────────────────────────────────────────────────────
pdf2image>=1.17.0
Pillow>=10.0.0
openpyxl>=3.1.0
python-dotenv>=1.0.0

# ── Local OCR mode (PaddleOCR + OpenCV) ──────────────────────────────────────
# CPU-only PaddlePaddle (no GPU required)
paddlepaddle>=2.6.0
paddleocr>=2.7.0

# OpenCV for checkbox / Likert-table detection
opencv-python>=4.9.0
numpy>=1.24.0

# ── RAG / Education Q&A web module ───────────────────────────────────────────
flask>=3.0.0
chromadb>=0.5.0
sentence-transformers>=3.0.0
PyMuPDF>=1.24.0
